// Type.
import { LowercaseLetter } from './lowercase-letter.type';
import { UppercaseLetter } from './uppercase-letter.type';
/**
 * @description A union of lowercase and uppercase letters, representing any letter in the alphabet.
 * @export
 */
export type AlphabeticLetter = LowercaseLetter | UppercaseLetter;
